Daily test records are generated by src/js/test-rankings.js from the Korean date and category ID. Each category receives 25–35 stable records per day; the next request after midnight gets a new set. The UI polls every 15 seconds. No cron job, daily deployment, or database inserts are needed. Every sample is labeled TEST / 테스트; real records are preserved. The old ranking-fixtures.json is unused.
